let nummer = 9
let string = "";

// übergabe des wertes Nummer an string und Konvertierung mit toString()
string = toString(nummer);

// Ausgabe des Typen
console.log(`Der Wert von nummer ist ${typeof nummer}. 
Der Wert von string ist ${typeof string}.`)