// leeres Objekt wird erstellt
let person = {};

// Eigenschaft name und alter wird ergänzt

person.name = "Friedrich";
person.alter = 25;

// änderung von name und alter
person.name = "Max Mustermann";
person.alter = 30;

// ausgabe ob alles richtig manipuliert wurde

console.log(person);
